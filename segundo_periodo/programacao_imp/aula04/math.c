#include <stdio.h>
#include <math.h>

int main () {
    double PI = 3.1416;
    double R = 0;
    while (R < = 6) {
        double volume = (4.0/3) * PI * pow(R, 3);
        printf("raio = %f ==> bolume - %f \n ", )
    }
    {
        /* code */
    }
    
}